import React, { Component } from 'react';
import { browserHistory } from 'react-router';

const ConversationSingle = ({params}) => {

  console.log('route params: ', params);

  return (
    <div>
      single:
    </div>
  );
}

export default ConversationSingle;
